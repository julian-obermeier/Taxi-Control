@extends('layouts.app', ['title'=>'SaaS-Pakete','heading'=>'SaaS-Pakete','eyebrow'=>'SaaS-Control-Center'])
@section('content')
<div class="toolbar"><p class="muted">Pakete steuern Preise, Limits und freigeschaltete Funktionsbereiche.</p><a class="btn btn-primary" href="{{ route('taxi-control.superadmin.packages.create') }}">Paket anlegen</a></div>
<section class="panel top-gap"><div class="table-wrap"><table><thead><tr><th>Paket</th><th>Monat</th><th>Jahr</th><th>Features</th><th>Mandanten</th><th>Status</th><th></th></tr></thead><tbody>@foreach($packages as $package)<tr><td><strong>{{ $package->name }}</strong><br><small>{{ $package->slug }}</small></td><td>{{ $package->price_monthly !== null ? number_format((float)$package->price_monthly,2,',','.') . ' €' : '–' }}</td><td>{{ $package->price_yearly !== null ? number_format((float)$package->price_yearly,2,',','.') . ' €' : '–' }}</td><td>{{ $package->features_count }}</td><td>{{ $package->subscriptions_count }}</td><td><span class="status-pill {{ $package->is_active ? 'success':'warning' }}">{{ $package->is_active ? 'Aktiv':'Inaktiv' }}</span></td><td><a class="btn btn-small" href="{{ route('taxi-control.superadmin.packages.edit',$package) }}">Bearbeiten</a></td></tr>@endforeach</tbody></table></div></section>
@endsection
